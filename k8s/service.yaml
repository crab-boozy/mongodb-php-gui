apiVersion: v1
kind: Service
metadata:
  name: mongodb-php-gui
  labels:
    app: mongodb-php-gui
spec:
  type: ClusterIP
  selector:
    app: mongodb-php-gui
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
